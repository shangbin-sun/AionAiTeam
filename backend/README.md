# backend

AI Team Python service (FastAPI). Mounted into AionCore at the corresponding
integration location via git submodule. During the frontend-first phase this
service is optional; the UI runs against an in-client MockTeamClient.
